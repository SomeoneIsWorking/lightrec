# Upstream provenance

- Upstream: `https://github.com/pcercuei/lightrec.git`
- Maintained fork: `https://github.com/SomeoneIsWorking/lightrec.git`
- Imported branch: `master`
- Imported commit: `550f700c037e8713e4567227514424621cd39cd7`
- Imported commit date: 2026-08-30
- Imported subject: `optimizer: Fix debug print format string`

The Git remotes are named `upstream` and `origin`, respectively. Future upstream
updates must retain the synchronous-JIT default, the typed fallback accounting,
and the target capability gates. A clean merge is not evidence that those
contracts survived; run the source and runtime contract tests after every
update.
